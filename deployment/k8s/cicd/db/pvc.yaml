apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: db
  name: db-pvc
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
