apiVersion: v1
kind: Service
metadata:
  name: management-api-svc
spec:
  type: LoadBalancer
  selector:
    app: management-api
  ports:
  - name: management-api
    port: 9000
    targetPort: management-api
    protocol: TCP
