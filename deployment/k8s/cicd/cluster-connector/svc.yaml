apiVersion: v1
kind: Service
metadata:
  name: cluster-connector-svc
spec:
  type: LoadBalancer
  selector:
    app: cluster-connector
  ports:
  - name: cluster-conn
    port: 9000
    targetPort: cluster-conn
    protocol: TCP
