# NOTE: no point in setting up patches for this as these configs are most likely going to change
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db-ss
spec:
  replicas: 1
  serviceName: db
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - name: db
        image: 'postgres:17-alpine'
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: "1000"
          requests:
            cpu: "200m"
        ports:
        - name: db 
          containerPort: 5432
        envFrom:
        - configMapRef:
            name: db-config
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -h
            - localhost
            - -U
            - postgres
          initialDelaySeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -h
            - localhost
            - -U
            - postgres
          initialDelaySeconds: 5
          timeoutSeconds: 1
        volumeMounts:
        # this is where data is stored inside the container, not host
        - name: db-volume-mount
          mountPath: /var/lib/postgresql/data
          
      volumes:
      - name: db-volume-mount
        persistentVolumeClaim:
          claimName: db-pvc